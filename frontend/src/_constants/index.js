export * from './alert.constants';
export * from './user.constants';

// Reducers and actions used predefined 'Constants' that act as labels to determine which actions are associated with each reducers. Housing them separately is a good practice so as not to misspell contstants across modules.

// Again, the index file compiles all modules in this directory so that all of the various functions from each module can be called easily.